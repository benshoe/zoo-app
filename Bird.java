public class Bird extends Animal {

  public Bird(String type, String name, int age, String enclosure) {
    super();
    setSpecies(Species.Bird);
    setType(type);
    setName(name);
    setAge(age);
    setEnclosure(enclosure);
  }
}